<nav class="breadcrumb">
  <%= link_to 'All Characters', characters_path, class: "breadcrumb-item" %>
</nav>

<div class="container col-md-6">
  <h3><%= @character.name %> (<%= link_to 'edit', edit_character_path(@character) %>)</h3>
  <p>
    <%= @character.backstory %>
  </p>
</div>
